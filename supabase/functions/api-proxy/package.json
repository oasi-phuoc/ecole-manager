{
  "name": "ecole-manager-api-proxy",
  "private": true,
  "description": "Dépendances locales pour le script bundle-api-proxy (esbuild uniquement).",
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
